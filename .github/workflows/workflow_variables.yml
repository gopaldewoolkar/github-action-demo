name: Testing Workflow Variables

om:
  workflow_dispatch

env:
  cloud: google-cloud

jobs:
  greeting_job:
  runs-on: ubuntu-latest
  env:
    Greeting: Hello
  steps:
      name: "Say Hello Mona it's Monday!"
      run: echo "$Grreting $First_Name is using $cloud !"
      env:
        First_name: Gopal
